# CLAUDE.md - Coding Profile
# Best for: dev projects, code review, debugging, refactoring
# Extends: Universal CLAUDE.md rules

---

## Output
- Return code first. Explanation after, only if non-obvious.
- No inline prose. Use comments sparingly - only where logic is unclear.
- No boilerplate unless explicitly requested.

## Code Rules
- Simplest working solution. No over-engineering.
- No abstractions for single-use operations.
- No speculative features or "you might also want..."
- Read the file before modifying it. Never edit blind.
- No docstrings or type annotations on code not being changed.
- No error handling for scenarios that cannot happen.
- Three similar lines is better than a premature abstraction.

## Review Rules
- State the bug. Show the fix. Stop.
- No suggestions beyond the scope of the review.
- No compliments on the code before or after the review.

## Debugging Rules
- Never speculate about a bug without reading the relevant code first.
- State what you found, where, and the fix. One pass.
- If cause is unclear: say so. Do not guess.

## Testing Rules
- After any code change, run the relevant tests/checks before declaring the task done. Do not ask the user to verify.
- Rust changes: `cargo test --manifest-path src-tauri/Cargo.toml --lib <filter>` for the affected module, plus `cargo check --manifest-path src-tauri/Cargo.toml` if the change crosses module boundaries.
- Frontend changes: `npx tsc --noEmit` and any relevant Vitest suite. For visual changes, see Styling Rules.
- If a test cannot be run in this environment, say so explicitly rather than skipping silently.
- When fixing a failing test, re-run only that test first, then the full module to confirm no regressions.

## Simple Formatting
- No em dashes, smart quotes, or decorative Unicode symbols.
- Plain hyphens and straight quotes only.
- Natural language characters (accented letters, CJK, etc.) are fine when the content requires them.
- Code output must be copy-paste safe.

## Styling Rules
- Never hardcode colors in component CSS. Use the semantic tokens defined in `:root` in `src/styles.css` (`--surface-*`, `--text-*`, `--border-*`, `--fill-*`, `--accent*`, `--warning*`, `--danger*`, `--chip-*`).
- If a needed color does not exist as a token, add it to both the `:root` block and the `@media (prefers-color-scheme: dark)` override — do not inline a hex/rgba in a component rule.
- Exceptions: pure `#fff`/`#000`, brand gradients, and launcher/splash-only one-offs that are intentionally theme-invariant. Comment the exception inline.
- When adding or modifying a component, visually verify both light and dark mode before declaring done. If dark mode cannot be tested, say so explicitly.
- Run `npm run check:colors` (or `./scripts/check-colors.sh`) on any CSS you touch. It flags raw hex/rgba in component rules. Migrate any new offenders to tokens before committing. Existing offenders are the Stage 4 migration backlog — don't add to them.

<!-- headroom:learn:start -->
## Headroom Learned Patterns
*Auto-generated by `headroom learn` on 2026-05-06 — do not edit manually*

### Large Files
*~18,000 tokens/session saved*
- `src/App.tsx` is very large (38,000–73,000 tokens depending on session) and CANNOT be read in full — always use `offset`/`limit` parameters or `Grep` to find specific sections first.
- `src-tauri/src/lib.rs` regularly hits 28k–73k tokens; the Read tool's hard cap is 25,000 tokens, so a full read will fail with `file_too_large`. Always Read with offset/limit or Grep first.
- `src-tauri/src/state.rs`, `src-tauri/src/tool_manager.rs`, `src-tauri/src/activity_facts.rs`, `src-tauri/src/proxy_intercept.rs`, and `src/styles.css` also routinely exceed 10k tokens. Read with offsets or grep for the symbol.

### Build & Check Commands
*~3,000 tokens/session saved*
- **Do not use `cd src-tauri && cargo ...`** — the shell's cwd frequently ends up pointing at `src-tauri` already (from a prior command), producing `no such file or directory: src-tauri`, or at the repo root producing `could not find Cargo.toml`. Always use the manifest path form: `cargo check --manifest-path src-tauri/Cargo.toml 2>&1 | tail -30` (and same for `cargo test`).
- `cargo test <a> <b>` treats the second name as an arg (`unexpected argument 'b' found`). Pass a single regex: `cargo test 'merge_daily\|merge_hourly'` or run separately.
- `cargo fmt --manifest-path src-tauri/Cargo.toml -- src/foo.rs` resolves `src/foo.rs` relative to CWD, not the manifest, and fails with `file does not exist`. Pass repo-root-relative paths (`src-tauri/src/foo.rs`) or run without explicit file args.

### Ports & Proxy Layout
*~800 tokens/session saved*
- 6767 = always-on Tauri intercept proxy (Rust); 6768 = managed headroom Python backend started by tool_manager. Never collapse the two — `headroom_proxy_reachable`/health checks should hit 6768 (backend), public stats endpoints are on 6767.
- The backend port is configurable via `backend_port.rs`; treat 6768 as the default, not a hard-coded constant when reading new code.

### Release Notes Path
*~600 tokens/session saved*
- The repo does NOT have a `.github/release-notes/` directory — `ls .github/release-notes/` errors with `No such file or directory`. The release workflow does not consume per-version notes files; release bodies are generated by `tauri-action` / GitHub at tag time. Do not invent or create a release-notes directory unless the workflow is changed to read from one.

### Environment
*~1,500 tokens/session saved*
- `find` via Bash does not accept `-not` / `-exec` (RTK wrapper blocks compound predicates). Use the Glob tool or simpler find invocations.
- `grep` compound patterns with unescaped `(` fail as `parentheses not balanced`. In zsh, escape as `\(test\)` or avoid parentheses in the regex.
- `rtk proxy <cmd> 2>&1 | tail -N` fails — `2>&1` is parsed as an arg to cargo/the inner command. Drop the pipe or use plain `cargo ... 2>&1 | tail`.

### Related Codebases
*~1,200 tokens/session saved*
- `headroom-web` Rails app lives at `/Users/garmlucassen/Code/headroom-web/` — contains mailers, Polar checkout service, API controllers
- Upstream Python repo is at `/Users/garmlucassen/Code/headroom/` (fork at `gglucass/headroom`, upstream `chopratejas/headroom`). Installed runtime lives under `~/Library/Application Support/Headroom/headroom/runtime/venv/lib/python3.12/site-packages/headroom/`.
- Headroom config/state: `~/Library/Application Support/Headroom/config/activity-facts.json` and `~/Library/Application Support/Headroom/memory.db`.
- Legacy path `~/.headroom/logs/proxy.log` still exists; current logs are at `~/Library/Application Support/Headroom/headroom/logs/`.

### Git: staging branch vs tag
*~800 tokens/session saved*
- A `staging` tag and `staging` branch both exist. `git push origin staging` fails with `src refspec staging matches more than one`. Always use the explicit refspec: `git push origin refs/heads/staging:refs/heads/staging`.
- Same issue applies to `git tag -d staging` / creating branches prefixed `staging/` (conflicts with `refs/heads/staging`).

### Python / JSON parsing
*~600 tokens/session saved*
- `python3` in Bash resolves to system Python 3.9 (from Xcode) which fails on modern JSON payloads (`Expecting property name enclosed in double quotes`). Use `/usr/bin/python3` only for simple parsing, and prefer `uv run python` from the `/Users/garmlucassen/Code/headroom/` repo when modern Python features are needed.

### Subagents
*~5,000 tokens/session saved*
- The user frequently rejects subagent (Explore) calls when Claude initiates them autonomously. Prefer doing direct Grep/Read/Glob exploration inline rather than spawning subagents unless the task clearly warrants it.

### Edit Tool Rules
*~2,500 tokens/session saved*
- Always `Read` a file before attempting to `Edit` it — editing without a prior read will fail with 'File has not been read yet'
- After an external linter modifies a file, re-read before editing again to avoid 'File has been modified since read' errors

### Key File Paths
*~2,000 tokens/session saved*
- Main Rust entry: `src-tauri/src/lib.rs` (Tauri commands, setup, tray)
- App state struct: `src-tauri/src/state.rs`
- Claude account/profile detection: `src-tauri/src/claude_account.rs`
- Keychain access: `src-tauri/src/keychain.rs`
- Python/tool bootstrap: `src-tauri/src/tool_manager.rs`
- Frontend: `src/App.tsx`, `src/styles.css`, `src/lib/types.ts`
- Build workflow: `.github/workflows/release-macos.yml`

### Browse Skill
*~2,000 tokens/session saved*
- The browse skill server runs on port 9400; if startup fails with 'port in use', run `lsof -i :9400 | head -5` then `kill <PID>` before retrying
- Use `B=~/.claude/skills/gstack/browse/dist/browse` and call `$B goto <url>` then `$B text` in the same shell command

### MCP Tool Usage
*~1,500 tokens/session saved*
- The headroom retrieve MCP tool is only available as `mcp__headroom__headroom_retrieve`, NOT as `headroom_retrieve` — calling it without the `mcp__headroom__` prefix will always fail with 'No such tool available'

<!-- headroom:learn:end -->
